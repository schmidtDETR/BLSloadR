# Check if Global Caching is Enabled via Environment Variable

Check if Global Caching is Enabled via Environment Variable

## Usage

``` r
check_bls_cache_env()
```

## Value

Logical value indicating if the environment variable USE_BLS_CACHE is
one of TRUE, YES, or 1
